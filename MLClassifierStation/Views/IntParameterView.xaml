<UserControl x:Class="MLClassifierStation.Views.IntParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:MLClassifierStation.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:NameConverter x:Key="NameConverter"></converters:NameConverter>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" Height="60">
        <Label Content="{Binding Name, Converter={StaticResource NameConverter}}" VerticalAlignment="Center"></Label>
        <TextBox Width="80" Text="{Binding Parameter, ValidatesOnNotifyDataErrors=True, NotifyOnValidationError=True}">            
        </TextBox>
    </StackPanel>
</UserControl>