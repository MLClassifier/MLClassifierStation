<UserControl x:Class="MLClassifierStation.Views.LearningResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MLClassifierStation.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="300"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <controls:InfoControl Grid.Column="0"
            InfoTitle="Model information" InfoContent="{Binding ModelInformation}"></controls:InfoControl>
        <controls:InfoControl Grid.Column="1"
            InfoTitle="Statistics" InfoContent="{Binding StatisticsInformation}"></controls:InfoControl>
    </Grid>
</UserControl>